source "https://rubygems.org"

# github-pages pins Jekyll and the whitelisted plugins to the exact
# versions GitHub runs in production, so local build == deployed build.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ no longer ships webrick, which `jekyll serve` needs locally.
gem "webrick"
